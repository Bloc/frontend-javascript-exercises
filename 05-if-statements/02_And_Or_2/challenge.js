// Let's get some more practice using conditionals. In this exercise, we're
// going to write a canIGet function that tells us if we can purcahse an item
// or not. The `canIGet` function should take two arguments:
//
// module.export.canIGet = function(item, money) {
// }
//
// The first argument represents what the user wants to buy.
//
// The second argument represents how much money they have. The method should
// evaluate if they have enough money to buy a computer or iPad. If they do have
// enough money for a computer or iPad, the method should return `true`,
// otherwise it should return `false`. In this example, we'll say that a computer costs
// $1000 and an iPad costs $500.
//
// An example for the first condition could be:
//
// if (item === "computer" && money >= 1000)
//
// Try to use `else if` rather than separate `if` statements.
//

module.export.canIGet = function(item, money) {
}