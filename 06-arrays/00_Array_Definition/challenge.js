// Create a function named `newArray`. It should take four arguments and return
// an array with those same arguments.

module.exports.newArray = function(a,b,c,d) {   
  // return an array consisting of the arguments here
};

// Create another function named `firstAndLast`. It should take one argument -
// an array - and return a new array with only the first and last objects of the
// argument:

module.exports.firstAndLast = function(a) {
  // return an array with only the first and last elements of the argument 
};